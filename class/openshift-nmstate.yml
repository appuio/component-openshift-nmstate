parameters:
  kapitan:
    compile:
      - input_paths:
          - ${_base_directory}/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - ${_base_directory}/component/main.jsonnet
          - ${_base_directory}/component/policies.jsonnet
          - ${_base_directory}/component/node-labels.jsonnet
        input_type: jsonnet
        output_path: openshift-nmstate/
